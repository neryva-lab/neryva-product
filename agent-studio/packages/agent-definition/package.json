{
  "name": "@neryva/agent-definition",
  "version": "0.1.0",
  "private": true,
  "description": "Immutable v1.schema.json parser/validator/compiler",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@neryva/agent-kernel": "workspace:*",
    "zod": "^3.24.0"
  }
}
