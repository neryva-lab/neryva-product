{
  "name": "@neryva/agent-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Pure bounded AgentRun state machine, budgets, step-id",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  }
}
