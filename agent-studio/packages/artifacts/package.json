{
  "name": "@neryva/artifacts",
  "version": "0.1.0",
  "private": true,
  "description": "Claim-check refs, reader/writer, checksums, encryption",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  }
}
