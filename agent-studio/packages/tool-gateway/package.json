{
  "name": "@neryva/tool-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Registry, effect/approval, idempotency, sandbox",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@neryva/contracts": "workspace:*",
    "@neryva/security": "workspace:*",
    "zod": "^3.24.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  }
}
