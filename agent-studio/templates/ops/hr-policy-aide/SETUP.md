# Setup — hr-policy-aide

1. **Install:** `POST /console/org/:orgId/assistants { name, template: { slug: "hr-policy-aide" } }`
2. **Connect knowledge:** ingest the `hr-handbook` corpus.
3. **Review the draft:** quote discipline, disclaimer text, routing rules.
4. **Evaluate:** 10 shipped cases — bar ≥ 9/10, 0 critical fails — then publish.
5. **Bind channels:** widget only (internal, sensitive surface).
