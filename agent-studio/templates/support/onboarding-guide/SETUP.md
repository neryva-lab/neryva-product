# Setup — onboarding-guide

1. **Install:**
   `POST /console/org/:orgId/assistants { name, template: { slug: "onboarding-guide" } }`
2. **Connect knowledge:** ingest the `getting-started` corpus in documented order.
3. **Review the draft:** step count, verification checks, banned claims.
4. **Evaluate:** 10 shipped cases — bar ≥ 9/10, 0 critical fails — then publish.
5. **Bind channels:** widget + messaging (text-first; setup needs reading).
